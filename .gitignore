# Secrets / credentials — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
GoogleService-Info.plist

# AWS
.aws-sam/
cdk.out/
cdk.context.json

# Node
node_modules/

# Media / recordings — host demos externally, keep the repo lean
recordings/
*.mp4
*.webm
*.mov

# OS
.DS_Store
._*
.AppleDouble
.LSOverride

# Xcode
*.xcodeproj/project.xcworkspace/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
build/
DerivedData/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
!default.pbxuser
!default.mode1v3
!default.mode2v3
!default.perspectivev3
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
timeline.xctimeline
playground.xcworkspace

# Swift Package Manager
.build/
Packages/
Package.resolved
*.xcodeproj

# CocoaPods
Pods/
*.xcworkspace

# Ingest script backups — data snapshots, not source
infra/ingest/*.backup.*.json
.next/

# Ingest manifests — build artifacts (pins/shopify catalogs), not source
infra/ingest/*.manifest.json

# ML working artifacts — datasets/models live in s3://giftmaxxing-dev-ml
infra/ml/.venv/
infra/ml/data/
infra/ml/model/
infra/ml/clusters.json
infra/ml/__pycache__/

# Xcode Cloud custom scripts — disabled (ci_post_clone was failing Archive)
ci_scripts/
