# The account wall: Apple + Google only. The guest door must stay dead —
# release testers each get a real, distinct profile.
appId: com.giftmaxxing.ios
name: sign-in wall — Apple/Google only, no guest door
tags:
  - smoke
---
- launchApp:
    clearState: true
    permissions:
      notifications: allow
    arguments:
      # Keychain sessions outlive clearState; the DEBUG hook signs out first.
      e2eReset: "1"

# A pristine guest may meet Maxi's consult before the wall (presentation
# order varies by iOS version) — skip it to reach the account gate.
- extendedWaitUntil:
    optional: true
    visible: "Skip for now"
    timeout: 12000
- runFlow:
    when:
      visible: "Skip for now"
    commands:
      - tapOn: "Skip for now"

- extendedWaitUntil:
    visible: "Sign in with Apple"
    timeout: 20000
- assertVisible: "Continue with Google"
- assertNotVisible: "Continue as Guest"
